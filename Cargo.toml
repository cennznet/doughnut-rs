[package]
name = "doughnut_rs"
version = "0.1.0"
authors = ["Centrality Developers <developers@centrality.ai>"]
edition = "2018"

[lib]
name = "doughnut_rs"

[features]
default = ["std"]
std = []

[dependencies]
hashbrown = { version = "0.3.0", features = ["nightly"] }
bit_reverse = "0.1.7"
schnorrkel = "0.1.0"
parity-codec = { version = "3.5.1", default-features = false}
